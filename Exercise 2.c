/*******************************************************************************************************/
 /* a program that writes a name 10 times, */
 /* each with a different loop style */
 /* a program that counts from one to ten, */
 /* prints the values on a separate line for each, */
 /* and includes a message of your choice when the count is 3 and a different message when the */
 /* count is 7. */
 /******************************************************************************************************/
main()
{
    int n;
    char Clinton;
    n = "Clinton";
    for (Clinton = 0; Clinton < 10; Clinton++)
    printf("%s\n",n);
    printf("Done!\n\n");
/* End of the for loop */

    int m;
    char Philip;
    m = "Philip";
        do{
            printf("%s\n",m); // The do..while loop
            Philip++;
        }
        while(Philip < 10);
printf("Done!\n\n");
// End of the do...while loop

    int o;
    char Akpan;
    o = "Akpan";
        printf("%s\n",o);
        Akpan++;
        while(Akpan < 10);













    }










